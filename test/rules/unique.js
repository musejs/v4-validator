"use strict";
var should = require('should');
var V4Validator = require('../../src/factory')();

describe('unique', function() {

    it('should not pass if no database is specified');

    it('should not pass if a table parameter is not specified');

    it('should pass when the field under validation is unique on a given database table');

    it('should not pass if the field under validation is not unique on a given database table');

});

