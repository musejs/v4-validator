"use strict";
var should = require('should');
var V4Validator = require('../../src/factory')();

describe('exists', function() {

    it('should not pass if no database is specified');

    it('should not pass if a table parameter is not specified');

    it('should pass when the field under validation exists on a given database table');

    it('should not pass if the field under validation does not exist on a given database table');

});
